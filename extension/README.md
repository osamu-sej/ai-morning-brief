# AI Morning Brief Chrome拡張

これは通常のGoogle Chromeで、利用者がすでにログイン済みのNotebookLMタブにだけ作用する開発用拡張です。Googleのパスワード、Cookie、トークンを読んだり保存したりしません。

開発中はChromeで`chrome://extensions`を開き、デベロッパーモードを有効にして「パッケージ化されていない拡張機能を読み込む」から、この`extension`フォルダを選択します。
